import { plantList } from "../datas/plantList";
function PlantItem({ plantName, plantCover, plantId, plantLight, plantWater }) {
    const plantName = plantName
    const plantCover = plantCover
    const plantId = plantId
    const plantLight = plantLight
    const plantWater = plantWater

    return
}